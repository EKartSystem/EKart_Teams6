@model E_Kart_Application.DTOs.ProductsDTO.ProductDto

<div class="container mt-5">
    <div class="card border-0 shadow-sm rounded-4 mx-auto" style="max-width: 700px;">
        <div class="card-header bg-primary text-white p-4 rounded-top-4">
            <h4 class="mb-0 fw-bold">Add New Product</h4>
        </div>
        <div class="card-body p-4">
            <form asp-action="Create" method="post">
                <div class="row g-3">
                    <div class="col-12">
                        <label asp-for="ProductName" class="form-label small fw-bold">Product Name</label>
                        <input asp-for="ProductName" class="form-control border-primary-subtle" required />
                    </div>
                    <div class="col-md-6">
                        <label asp-for="UnitPrice" class="form-label small fw-bold">Unit Price (₹)</label>
                        <input asp-for="UnitPrice" type="number" step="0.01" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label asp-for="UnitsInStock" class="form-label small fw-bold">Initial Stock</label>
                        <input asp-for="UnitsInStock" type="number" class="form-control" />
                    </div>
                    <div class="col-md-6">
                        <label asp-for="CategoryId" class="form-label small fw-bold">Category</label>
                        <select asp-for="CategoryId" class="form-select"
                                asp-items="@(new SelectList(ViewBag.Categories, "CategoryId", "CategoryName"))">
                            <option value="">-- Select Category --</option>
                        </select>
                    </div>
                    <div class="col-md-6">
                        <label asp-for="SupplierId" class="form-label small fw-bold">Supplier</label>
                        <select asp-for="SupplierId" class="form-select"
                                asp-items="@(new SelectList(ViewBag.Suppliers, "SupplierId", "CompanyName"))">
                            <option value="">-- Select Supplier --</option>
                        </select>
                    </div>
                    <div class="col-12 mt-4 d-flex justify-content-between">
                        <a asp-action="Index" class="btn btn-link text-muted px-0">Cancel</a>
                        <button type="submit" class="btn btn-primary px-5 rounded-pill">Save Product</button>
                    </div>
                </div>
            </form>
        </div>
    </div>
</div>